{
  "name": "give-me-five-editor",
  "version": "3.11.1",
  "private": true,
  "description": "Local portrait-video editor for the Give Me Five workflow.",
  "packageManager": "pnpm@11.9.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check transcribe-worker.js && node --check render-timing.js && node --check marker-analysis.js && node --check visual-entry.js && node --check music-suitability.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=24 <25"
  },
  "dependencies": {
    "@huggingface/transformers": "4.2.0",
    "express": "5.2.1",
    "fft.js": "4.0.4",
    "ffmpeg-static": "5.3.0",
    "ffprobe-static": "3.1.0",
    "multer": "2.2.0"
  }
}
